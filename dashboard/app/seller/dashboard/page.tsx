export default function SellerDashboardPage() {
  return (
    <div className="space-y-6">
      <h1 className="text-2xl font-semibold tracking-tight">Merchant Dashboard</h1>
      <p className="text-sm text-muted-foreground">
        BACKEND TASK: Add OpenAPI response schemas for seller dashboard endpoints (e.g. analytics KPIs and recent orders)
        and wire this view using real APIs.
      </p>
    </div>
  );
}
