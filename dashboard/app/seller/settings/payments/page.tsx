export default function SellerPaymentsSettingsPage() {
  return (
    <div className="mx-auto max-w-2xl space-y-6">
      <h1 className="text-2xl font-semibold tracking-tight">Payments Settings</h1>
      <p className="text-sm text-muted-foreground">
        BACKEND TASK: Add OpenAPI response schemas for payment settings endpoints (per provider) and ensure secrets are
        masked server-side.
      </p>
    </div>
  );
}
